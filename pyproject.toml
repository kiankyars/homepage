[project]
name = "outreach"
version = "0.1.0"
description = "Minimal funnel for finding and contacting Bay Area investors and frontier lab folks"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "email-validator>=2.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
